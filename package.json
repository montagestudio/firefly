{
  "name": "firefly",
  "private": true,
  "version": "0.0.0",
  "description": "Server for filament in the browser",
  "main": "index.js",
  "scripts": {
    "test": "jshint . && istanbul test jasmine-node -- spec",
    "start": "node index.js --client=../filament",
    "update-dependencies": "rm -rf node_modules && npm install && npm dedupe && git add --update node_modules && git add node_modules"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/declarativ/firefly.git"
  },
  "dependencies": {
    "joey": "~1.3.1",
    "optimist": "~0.6.0",
    "q-io": "~1.10.4"
  },
  "devDependencies": {
    "jasmine-node": "~1.11.0",
    "istanbul": "~0.1.44",
    "jshint": "~2.3.0"
  },
  "subdomain": "firefly",
  "engines": {
    "node": "0.10.x"
  }
}
