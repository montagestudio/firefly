# project server

description "Firefly Project Server"
author "Stuart Knightley <stuart@declarativ.com>"

# Start service when the network is available or, in development, when app
# mountpoint is available
start on runlevel [2345] or vagrant-mounted MOUNTPOINT=/srv/firefly

# Normal shutdown
stop on runlevel [016]

# Automatically restarting the process if it crashes is handled by Naught

setuid montage
setgid docker
chdir /home/montage

pre-start script
    echo "Remove a leftover naught.ipc"
    rm -f /home/montage/naught.ipc
    echo "Test existence of server"
    # This exits with a non-zero status if the server isn't found and stops
    # the server launch
    test -f /srv/firefly/project.js
    echo "okay"
end script

script
	export NODE_ENV="production"
	export GITHUB_CLIENT_ID="4b81fbc3787758f90e9a"
	export GITHUB_CLIENT_SECRET="3f34e79d53c503cf4ae0e6e6a7d816a08ac561a4"
	export FIREFLY_APP_URL="http://aurora.montagestudio.com"
	export FIREFLY_PROJECT_URL="http://project.montagestudio.com"
	# Start the process
	exec /usr/bin/naught start --daemon-mode false --cwd /srv/firefly /srv/firefly/project.js --client=/srv/filament --directory=/srv/clone

end script
