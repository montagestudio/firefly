{
  "name": "npm",
  "version": "1.0.0",
  "description": "Service for npm related package operations on a workspace.",
  "main": "index.js",
  "dependencies": {
    "body-parser": "^1.18.2",
    "cors": "^2.8.4",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-spies": "^1.0.0",
    "concurrently": "^3.5.1",
    "http-server": "^0.11.1",
    "mocha": "^5.1.1",
    "open": "0.0.5",
    "supertest": "^3.0.0"
  },
  "scripts": {
    "build": "docker build -t firefly-npm .",
    "test": "npm run build && docker run --entrypoint mocha firefly-npm",
    "doc": "concurrently 'http-server -p 2450' 'open http://localhost:2450/doc'"
  },
  "license": "ISC"
}
