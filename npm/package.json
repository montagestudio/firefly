{
  "name": "npm",
  "version": "1.0.0",
  "description": "Service for npm related package operations on a workspace.",
  "main": "index.js",
  "dependencies": {
    "body-parser": "~1.1.0",
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "npm": "2.15.11",
    "q-io": "^1.13.5",
    "semver": "2.2.1"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-spies": "^1.0.0",
    "concurrently": "^3.5.1",
    "http-server": "^0.11.1",
    "mock-fs": "^4.5.0",
    "open": "0.0.5",
    "supertest": "^3.0.0"
  },
  "scripts": {
    "build": "docker build -t firefly-npm --rm .",
    "test": "npm run build && docker run --entrypoint mocha --rm firefly-npm",
    "doc": "concurrently 'http-server -p 2450' 'open http://localhost:2450/doc'"
  },
  "license": "ISC",
  "engines": {
    "node": "4.8.7",
    "npm": "2.15.11"
  }
}
